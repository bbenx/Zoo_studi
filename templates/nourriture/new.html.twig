{% extends 'base_admin.html.twig' %}

{% block title %}Nouvelle Nourriture{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Créer une nouvelle nourriture</h1>
        <a href="{{ path('app_nourriture_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
    </div>

    {{ include('nourriture/_form.html.twig', {'button_label': 'Créer'}) }}
{% endblock %}
