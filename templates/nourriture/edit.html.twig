{% extends 'base_admin.html.twig' %}

{% block title %}Modifier Nourriture
{% endblock %}

{% block body %}
	<div class="d-flex justify-content-between align-items-center mb-4">
		<h1>Modifier nourriture</h1>

		<a href="{{ path('app_nourriture_index') }}" class="btn btn-secondary mr-2">Retour à la liste</a>
		{{ include('nourriture/_delete_form.html.twig') }}
	</div>

	{{ include('nourriture/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{% endblock %}
