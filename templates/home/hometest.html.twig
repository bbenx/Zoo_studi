<nav class="navbar navbar-expand-lg fixed-top">
    <div class="container-fluid">
        <button 
        class="navbar-toggler" 
        type="button" 
        data-toggle="collapse" 
        data-target="#navbarNav" 
        aria-controls="navbarNav" 
        aria-expanded="false" 
        aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand d-flex align-items-center" href="{{ path('home') }}">
            <img src="images/logo.png" alt="Zoo Logo" style="height: 40px;">
        </a>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item {% if current_page is defined and current_page == 'home' %}active{% endif %}">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#animals">Animaux</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#habitats">Habitats</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#services">Services</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto d-flex align-items-center">
                <li class="nav-item" id="dashboard-item">
                    {% if app.user %}
                        <a class="nav-link custom-button" href="{{ path('app_admin_dashboard') }}">Dashboard</a>
                    {% endif %}
                </li>
                <li class="nav-item" id="logout-item">
                    {% if app.user %}
                        <a class="nav-link custom-button power-button" href="{{ path('app_logout') }}"><i class="fas fa-power-off"></i></a>
                    {% else %}
                        <a class="nav-link custom-button" href="{{ path('app_login') }}">Connexion</a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>