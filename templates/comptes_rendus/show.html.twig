{% extends 'base.html.twig' %}

{% block title %}ComptesRendus{% endblock %}

{% block body %}
    <h1>ComptesRendus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comptes_rendu.id }}</td>
            </tr>
            <tr>
                <th>EtatAnimal</th>
                <td>{{ comptes_rendu.EtatAnimal }}</td>
            </tr>
            <tr>
                <th>DetailEtat</th>
                <td>{{ comptes_rendu.DetailEtat }}</td>
            </tr>
            <tr>
                <th>TypeNourriture</th>
                <td>{{ comptes_rendu.TypeNourriture }}</td>
            </tr>
            <tr>
                <th>GrammageNourriture</th>
                <td>{{ comptes_rendu.GrammageNourriture }}</td>
            </tr>
            <tr>
                <th>DatePassage</th>
                <td>{{ comptes_rendu.DatePassage ? comptes_rendu.DatePassage|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ comptes_rendu.creationDate ? comptes_rendu.creationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>ModificationDate</th>
                <td>{{ comptes_rendu.modificationDate ? comptes_rendu.modificationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comptes_rendus_index') }}">back to list</a>

    <a href="{{ path('app_comptes_rendus_edit', {'id': comptes_rendu.id}) }}">edit</a>

    {{ include('comptes_rendus/_delete_form.html.twig') }}
{% endblock %}
