<form method="post" action="{{ path('app_comptes_rendus_delete', {'id': comptes_rendu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comptes_rendu.id) }}">
    <button class="btn">Delete</button>
</form>
